Fetch the Jira ticket $ARGUMENTS from the NOTIFY project (cloudId: 10c36cea-3e65-4c45-a685-2cc535be6893) and set up the implementation context.

Steps:
1. Fetch the ticket using the Atlassian MCP tool and read the full description, acceptance criteria, and definition of done.
2. Summarise the ticket in 2-3 sentences: what needs to be built and why.
3. List the acceptance criteria as a checklist.
4. Identify which files/modules in the codebase are likely to be touched (search `src/` for relevant existing code).
5. State any blockers or dependencies on other tickets before starting.
6. Ask for confirmation before writing any code.
